#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main() {
    int target, guess;
    
    srand(time(
    
   

  
0));
    target = rand() % 
    target = rand() % 

    target = rand()

    target = 

    target

    ta

  
100 + 1;  // 1부터 100 사이의 랜덤 숫자
    
    
    
   
printf("1부터 100 사이의 숫자를 맞춰보세요!\n");
    
    
    
   
do {
        printf("숫자를 입력하세요: ");
        
        scan

       
scanf("%d", &guess);
        
        
        
        

        
   
if (guess < target)
            
        

     

  
printf("더 큰 수입니다.\n");
        else if (guess > target)
            printf("더 작은 수입니다.\n");
        else
            printf("정답입니다!\n");
        
    } while (guess != target);
    
    return 0;
}
